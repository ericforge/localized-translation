name: validate-localization

on:
  push:
  pull_request:

jobs:
  validator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Run validator tests
        run: bash tests/test_validator.sh
