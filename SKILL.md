---
name: localized-translation
description: 多语言本地化翻译流水线（翻译 → 校对 → 本土化 → 润色 → 可选合规审核），支持任意语言对与地区变体（zh-CN/zh-TW/en-US/en-GB/es-ES/es-MX/pt-BR/de-DE/fr-FR/ja-JP/ko-KR/ar-SA 等）。当用户要求"翻译/汉化/英译中/中译英/localize/本地化/写成某种语言/多语言版本/翻成德语日语"时使用，尤其适用于跨境电商：Listing 文案、广告、独立站、App 文案、说明书与合规文本。产出不是字面对应的译文，而是目标市场读者读起来像原生文案的终稿，并附术语表与待确认项。
agent_created: true
---

# 多语言本地化翻译（Localized Translation）

## 核心立场

1. **翻译 ≠ 本地化。** 字面忠实只是及格线，目标是让目标市场读者觉得「这就是用母语写的」。
2. **语言不可知。** 流水线不绑定语言对 —— 角色能力用「目标语言母语级」定义，而非「中文系 / 英文母语」。
3. **locale 精确到「语言-地区」，且不要按洲套规则。** `es-ES ≠ es-MX`，`pt-BR ≠ pt-PT`，`zh-CN ≠ zh-TW`，`en-US ≠ en-GB`；数字格式上瑞士 `1'234.56`、英国 `1,234.56` 都不是「欧洲逗号」那一套。
4. **默认不走第三语言中转。** 除非用户明确授权，并显式填写 `pivot_locale`、标注失真风险并完成额外验证。
5. **先落 locale，再动笔。** 只给语言时按默认值开跑，但把默认写进「待确认」。
6. **URL、IP 与网络地址默认逐字符冻结。** 它们是语言无关的标识符；经确认的 slug 本地化或换指向属于独立的链接变更，不得伪装成翻译。详见 `references/urls-and-links.md`。

## 参考文件索引（按需读，别一口气全读）

| 文件 | 什么时候读 |
| --- | --- |
| `references/localization-checklist.md` | **每次都要过**，本土化阶段的主清单 |
| `references/locale-profiles.md` | 定了目标 locale 后，先查这个 locale 的语言档 |
| `references/industry-labels.md` | 涉及食品/化妆品/纺织/电子/玩具/化学品/电池，或需用销售国官方语言时 |
| `references/engineering.md` | 交付物是语言包 / JSON / 代码 / 多语言站点，或涉占位符、复数、编码时 |
| `references/case-orthography.md` | 涉关键词 / 标题 / 品牌名 / 产地表述，或目标语不是英语时（**大小写在各语系承载的信息量不同**） |
| `references/typography-and-scripts.md` | 涉标点符号、排版、多语言字体、简繁转换、阿语数字体系时（**符号用错=一眼看出是外国人写的**） |
| `references/urls-and-links.md` | 文本或文件里**含 URL / IP / 域名 / 文件路径 / 邮箱**，或涉及多语言站点、SEO、投放链接、网络配置时（**URL 与 IP 逐字符冻结，但「换指向」最容易被漏**） |

## 流水线：四个角色 + 一个可选角色

| 角色 | 目标 | 产出 | 对应执行步 |
| --- | --- | --- | --- |
| R1 翻译专家（按源语与目标语的母语级标准执行；必要时安排人工母语审校） | 忠实原文、行文自然 | 初稿 | Step 1 |
| R2 校对编辑（按**目标语言母语级标准**） | 语法、用词、术语一致 | 二稿 | Step 2 |
| R3 **本土化编辑**（依据**目标市场资料，必要时交目标市场母语者审校**） | 单位货币、文化、敬语、复数占位符、排版、合规词 | 三稿 | Step 3 |
| R4 润色专家（**目标语**写作专家） | 按文体提升美感或销售力 | 终稿 | Step 4 |
| R5 合规审核（可选，受监管品类才启用） | 当地法规与平台政策签注 | 合规签注 | Step 4 后 |

> 这些角色是**提示词技法**，用来让模型分工；不是真实资质，也别在交付物里向客户声称。

关键机制：**每段只做一件事，并以上一稿为输入**（「输出物」= 交接契约），不要一次成稿。

## 执行流程

### Step 0：先落 locale 与适用法域

确定 8 项，写进输出头部：**源 locale / 目标 locale / 文体 / 受众 / 目的 / 平台或载体 / 销售市场与发布法域 / 禁改项**。

- 只给语言时按此默认并在「待确认」标注：英文→`en-US`、中文→`zh-CN`、西语→`es-ES`、葡语→`pt-BR`、法语→`fr-FR`
- 低风险短文本可以使用默认 locale，但必须标记 `ASSUMED_DRAFT`。
- 投放、上架、付款、医疗、电气、法律、标签或受监管品类，无论长度都必须先确认目标市场、平台/载体和适用法域；缺失时状态为 `BLOCKED`。
- 禁改项 = 品牌名、SKU、型号、专利号、法规编号、**URL（默认逐字符冻结：协议、域名、端口、路径标识段、参数名与值、追踪参数）**、**IP 地址（IPv4/IPv6，含端口、CIDR、掩码）与 MAC 地址**、占位符、参数标签名；经批准的 slug 本地化或换指向另行记录

### Step 0.5：确认「载体」，别只翻手头那段文字

| 载体 | 处理 |
| --- | --- |
| 正文文本 | 本流水线主对象 |
| **图片内文字**（主图、A+ 图、包装、说明书扫描件） | 用 Read 读图提取图中文字，一并给出替换文案，并提示需重新出图 |
| 视频 / 字幕 | 字幕按时轴给；注意语速与时长（德语比英语长约 30%，需留时长） |
| 结构化文件（JSON / XLSX / CSV / po / ARB） | **只翻 value，不动 key**；见 `references/engineering.md` |
| **文本或文件里含 URL / IP / 邮箱 / 文件路径** | 先抽出全部地址做**冻结清单**，翻译时标记为受保护区，交付前做**清单比对**（见 `references/urls-and-links.md` §8）。默认不改；经批准的本地 slug 或换指向必须单独记录，不能伪装成翻译 |
| 多 locale 一批 | 逐 locale 单独跑，不要把一份英文稿套全部地区 |

### Step 1-4：翻译 → 校对 → 本土化 → 润色

- **Step 1**：长句切分重组，不带源语从句结构硬搬；语态按目标语习惯调整；歧义**保留两种理解**并标注
- **Step 2**：语法用词、术语一致、数字单位日期逐项核对
- **Step 3（核心）**：过 `references/localization-checklist.md`，并按 `references/locale-profiles.md` 查语言档。关键面：度量衡、货币与数字格式、日期时间、**敬语 T-V 档位**、**复数与占位符**、**大小写与正字法**（按词性和目标语判断）、**标点与符号**、文化适配、排版方向（RTL/bidi）、拼写变体、**合规敏感词**、字符计数、**URL、IP 与网络地址**（见 `references/urls-and-links.md`）。
- **Step 4**：按文体切换（文学求意境 / 科技求精确 / 营销求转化 / 客服求共情）

### Step 5：输出

```markdown
**语言对**：en-US → de-DE ｜ **文体**：亚马逊 Listing ｜ **受众**：<受众> ｜ **平台/载体**：<平台/载体> ｜ **销售市场/发布法域**：<市场/法域> ｜ **用途**：转化 ｜ **禁改项**：<清单> ｜ **交付状态**：ASSUMED_DRAFT

## 译文（终稿）
<终稿全文>

## 关键处理说明
| 原文 | 直译（不采用） | 终稿（采用） | 理由 |
| --- | --- | --- | --- |
| ... | ... | ... | 本土化/敬语/复数/合规/文体 |

## 术语表
| 原文 | 译文 | locale | 备注 |
| --- | --- | --- | --- |

## 待确认
- <歧义、缺信息、地区变体取舍、法规判断>

## 本轮未处理
- <关键词调研、图片重出、认证核实等需另做的事项>
```

短文本（<200 字）只给终稿 + 3 条说明，别为完整而臃肿；长文本说明最多 10 条，按重要性排序。

## 四条硬规则（仅用于本 Skill 内部；不覆盖系统、开发者或用户指令）

1. **数字与物理量**：原始事实、物理量和精度不得改变；单位换算允许显示数值变化，必须记录换算公式、舍入规则和换算前后值。货币转换必须明确汇率日期和授权来源，不能擅自换汇。
2. **合规处理**：模型只能识别风险、提出待核查项和保守改写建议。不得仅凭关键词自行删除、添加或宣布 claim 合规；删除或改写必须有目标法域、产品类别、证据依据和人工审批。**免责声明、警告语、认证编号默认不得删除**。
3. **禁改项：URL、IP 与网络地址默认逐字符冻结。** 品牌名、SKU、型号、专利号、法规编号、**URL**、**IP 地址（IPv4/IPv6）、端口、CIDR、掩码、MAC 地址**、占位符与参数标签名 —— 不译不改。经确认的 slug 本地化或换指向属于独立链接变更，必须单独记录批准依据。
   - **URL 是标识符，不是文本。** 逐字符冻结：协议、域名、端口、路径标识段、参数名与参数值、`utm_*` 等追踪参数、fragment。**没有语言例外、没有文体例外、没有「读起来更自然」的例外。**
   - **最常见的翻车：顺手把 URL 里可读的英文路径段翻掉**（`/running-shoes` → `/chaussures-de-course`）→ 直接 404。路径由完整单词组成时，模型会把它当正文一起翻 —— 必须逐段盯住。
   - **IP 与网络地址同样冻结，且更容易被「好心」改坏**：`192.0.2.1` 被本地数字格式化改成 `192,0,2,1`；`:8080` 因法语排版规则被加成 ` : 8080`；`8000-8080` 的连字符被换成 en dash；MAC 被转小写或改用连字符；IPv6 的 `::` 缩写被「补全」；**`localhost` 被当普通英文词翻译**。
   - **根因**：不要把 locale 的数字格式化 / 标点规范化 / 大小写转换套到 IP 上 —— 对价格是对的，对 IP 是破坏性的。IP 走「标识符」通道，不走「数字」通道。
   - 同类的还有：无协议地址（`www.example.com`）、裸域名、邮箱、`mailto:` / `tel:`、文件路径、**嵌套编码 URL**（`?redirect=https%3A%2F%2F…`，不要解码再编码）—— 一律冻结。什么算地址见 `references/urls-and-links.md` §0.3、§0.4。
   - **「不译」不等于「什么都不做」**：**href 不译而锚文本必须译**；目标市场有本地版本时应**换指向**（换链接，不是翻 URL）；slug 是否本地化属站点策略，**默认保留**并写进「待确认」。
   - **交付前必须做地址清单比对**（源文与译文的 URL / IP / MAC 抽出来逐字符 diff），这是唯一能客观证明没翻坏的手段。做法见 `references/urls-and-links.md` §8。
4. **不新增、不删减事实**：不得新增原文没有的数据、功效或宣称。发现目标市场缺少法定警示或标签要素时，停止“可发布”结论，输出 `BLOCKED` 和待补资料清单，由当地合规负责人决定是否补充。

## 翻译之外必须同步做的 7 件事（最容易漏）

翻译做对了，这些没做仍然会出事。做完流水线后逐项确认：

1. **官方语言要求必须按市场 × 品类 × 载体 × 渠道核验。** 不能把广告、商品页、标签、说明书和清关文件视为同一规则。详见 `references/industry-labels.md`。
2. **关键词不能假定可直译。** Amazon Search Terms、站内搜索词和 SEO 文案必须在目标市场重新验证（当地叫法、搜索量、同义词）。`meta keywords` 不是 Google 当前排名信号，不要把它与上述字段混为一谈。**不要输出未经验证的「关键词」**，标注为待调研。
   - 附：**大小写不能用来区分词义** —— 亚马逊/Google 的匹配不区分大小写，`polish` 与 `Polish` 是同一个 token。多义词必须用**上下文词组**消歧（`nail polish` vs `made in Poland`），靠大小写堆词无效且被视为重复。详见 `references/case-orthography.md`。
3. **图片内的文字。** 主图、A+ 图、包装、说明书扫描件里的硬编码文字不翻，等于没本地化。需给替换文案并提示重新出图。
4. **受管制品类的强制标签。** 食品过敏原与营养表、化妆品成分命名（按目标市场法定或认可名称，并保留 INCI 对照）、纺织纤维成分、CLP 象形图、WEEE/电池标识、原产地标示 —— 文字层之外还有标签层。
5. **商标与语义排查。** 品牌名/产品名进入新市场前查：是否已被注册、当地是否有负面或滑稽含义、是否需要转写（日文片假名等）。**注意：广为流传的「Nova = no va」案例已被多方质疑，请当作「需要实查」的提示而非事实。**
6. **法定条款随市场变。** 保修、退换货、免责措辞、发票字段、隐私政策各有属地要求 —— 源市场表述直接搬过去可能无效甚至违法。
7. **地区名称、地图、旗帜、时区表述必须按适用政策处理。** 只有当发布法域、销售市场、平台政策或用户明确指定中国合规口径时，才启用相应中国官方表述；否则以目标市场法律、平台政策和项目品牌规范为准。拿不准的写进「待确认」，不要赌。

## 输出语言约定

- 说明表、待确认、理由 → 用当前会话语言；无法判断时再询问用户
- 终稿与术语表译文 → 目标语言原样呈现，不做二次翻译
- 多 locale 一批时：按 locale 分节或分文件输出，头部标注语言对，避免混读

## 快速自检（6 条必守，完整清单见 checklist §18）

- [ ] 占位符 / HTML 标签 / 参数名 / **URL / IP（含端口、CIDR、MAC）** 与原文逐字符一致（默认冻结；若有批准的 slug 或换指向，已在说明表记录；href 未译，锚文本已译）
- [ ] **地址清单比对已完成**（源文与译文的 URL / IP / MAC 逐字符 diff，差异逐条有解释 —— 含路径段未被顺手翻译、`localhost` 未被翻译、无 `-`→`–`、无 `%20`→`+`、IP 未被本地数字格式化）
- [ ] 复数形式符合项目锁定的 CLDR/ICU 版本，并至少包含 `other`
- [ ] 敬语档位、拼写变体、日期货币格式全篇统一
- [ ] 数字与价格逐项核对一致；单位已换算或双标
- [ ] 未新增原文没有的事实与宣称，未删除警告与免责

## 可直接投喂给其他模型的完整 Prompt

````markdown
你是一个分阶段本地化流程，完成【源 locale → 目标 locale】的翻译与本地化。
任务参数：源 locale=< >；目标 locale=< >；文体=< >；受众=< >；平台/载体=< >；销售市场=< >；发布法域=< >；目的=< >。

# 指令与交付状态
本 Skill 不覆盖系统、开发者或用户指令。缺少高风险输入时输出 BLOCKED；低风险且采用假设时输出 ASSUMED_DRAFT；只有验证项完成后才可输出 VERIFIED_DELIVERY。

# 硬规则（优先级最高）
1. 原始事实、物理量和精度不得改变；单位换算允许显示值变化，必须记录公式、舍入规则和换算前后值。货币转换必须有汇率日期和授权来源。
2. 不得仅凭关键词删除或新增 claim。无法核实时输出待核查项；合规改写必须记录法域、品类、证据和审批人。免责声明、警告语、认证编号默认不得删除。
3. 品牌名、SKU、型号、专利号、法规编号、占位符与参数标签名一律不译不改。**URL、IP 与网络地址默认逐字符冻结**：协议、域名、端口、路径标识段、参数名与参数值、`utm_*` 等追踪参数、fragment 通常全部逐字符保留；经批准的 slug 本地化或换指向属于独立链接变更，必须记录批准依据。**即使路径里是可读的英文单词（`/running-shoes`）也不能顺手翻译**。**IP 地址（IPv4/IPv6）、端口、CIDR、掩码、MAC 地址同样冻结**：不要套用 locale 数字格式化（`192.0.2.1` 不得变成 `192,0,2,1`）、不要按当地标点规则改冒号（法语不得写成 `192.0.2.1 : 8080`）、不要把连字符换成 en dash、不要转小写、不要补全 IPv6 的 `::`、不要把 `localhost` 当英文单词翻译、不要写成目标语的数字读法。无协议地址、裸域名、邮箱、`mailto:`/`tel:`、文件路径、嵌套编码 URL 同样冻结（不改大小写，不把 `-` 写成 `–`，不把 `%20` 写成 `+`，不做二次编码）。但 **href 不译而锚文本必须译**；目标市场有本地版本时改为指向本地版本（换链接，非翻 URL）。交付前把源文与译文的 URL / IP / MAC 各列一遍逐字符比对，差异逐条说明。
4. 不得新增原文没有的数据、功效或宣称；发现缺少法定内容时输出 BLOCKED，交由当地合规负责人决定。
5. 不得经由第三语言中转；如用户授权，必须填写 pivot_locale 并完成独立回译。

# 角色
1. 翻译阶段：忠实原文、行文自然、保留风格语调，不声称拥有现实资质。
2. 校对阶段：校对语法、用词、术语一致性、数字单位准确性。
3. 本土化阶段：处理度量衡、货币与日期格式、文化适配、敬语体系、复数形式与占位符、排版方向和地区变体；只提出合规风险，不擅自宣布或删除 claim。
4. 润色专家：目标语言写作专家。输出【终稿】：按文体提升美感或销售力（文学求意境，科技求精确，营销求转化，客服求共情）。
5.（可选，受监管品类启用）合规审核：核查目标市场所需的认证表述、警示语、责任人信息与强制标签，输出【合规签注】。

# 流程（每段必须基于上一稿修改，不得重翻）
初稿 → 二稿 → 三稿 → 终稿 →（可选）合规签注

# 输出格式
1. 终稿全文
2. 关键处理说明表：原文 / 直译(不采用) / 终稿(采用) / 理由
3. 术语表：原文 / 译文 / locale / 备注
4. 待确认清单：歧义、地区变体取舍、法规判断
5. 自检结果：每项标记 PASS / FAIL / NOT_RUN / NOT_VERIFIED，并列出工具、来源或缺失项
6. 交付状态：BLOCKED / ASSUMED_DRAFT / VERIFIED_DELIVERY
````

## 场景变体

- **Listing 本地化（多站点）**：先按 `references/locale-profiles.md` 站点表确认 locale 与货币，再定字段上限与禁词；德/法/西站标题比英文站更易超限需压缩；日本站卖点文案默认敬体（`です・ます`）；**关键词另行验证，不可直译**。
- **App / UI 文案**：优先处理占位符、复数、按钮长度膨胀；RTL 需附镜像布局说明。见 `references/engineering.md`。
- **说明书/合规文本**：安全警告一条不能省；义务层级必须结合原始标准、版本和上下文核验，不能只按 `shall / must / should / may` 固定映射。用错层级等于改变责任范围，属实质错误。注意销售国官方语言要求。
- **排版与符号**：引号、破折号 `——`、省略号 `……`、顿号、连字符三级、不可断空格、汉字字形、数字体系等**只暴露「不是母语者写的」**的细节，见 `references/typography-and-scripts.md`。
- **含链接的文案 / 独立站与 SEO**：URL/IP 默认逐字符冻结；slug 本地化、换指向和语言切换是独立的站点变更，需记录批准依据。优先使用可直接访问、可抓取的语言 URL；不要仅依赖 IP 自动跳转。`hreflang` 规则见 `references/urls-and-links.md`。
- **技术文档 / 说明书 / 网络配置**：日志行、防火墙规则、DNS 与 SPF 记录、子网与掩码表、设备后台说明里的 IP / 端口 / MAC 是**最高危对象** —— 它们看起来就是普通数字，最容易被「本地化」流程改坏。IPv6 的冒号与 `::` 缩写尤其要注意，RTL 语种下还需 bidi 隔离。见 `references/urls-and-links.md` §0.4。
- **客服与供应商邮件**：语气优先、信息次之；订单号与日期保留原文可追溯。
- **法律合同**：只出参考稿，末尾标注「非法律意见，需当地执业律师审校」。

## 关于 Pivot（中转）翻译

默认禁止。仅在源语与目标语资源极稀缺、且用户明确同意时启用（源 → 指定 `pivot_locale` → 目标）。启用时必须标注风险、核对关键术语与数字，并由独立会话完成回译；无法回译时只能交付草稿。

## 回译验证的做法

- **换一个独立会话/子代理做回译**，不要让同一会话既翻又回译（会自我锚定，看不出问题）
- 回译后与原文逐句比对，区分：**合理本地化**（可接受，说明清楚）vs **语义丢失**（必须修）
- 必做场景：高价值 Listing 标题与首条卖点、合规警示语、含数字/单位/价格/法律表述的段落、pivot 产出的关键句

## 持续优化：沉淀个人词库

- 术语表和记忆应写入宿主提供的可配置工作区；不要假设 `~/.workbuddy`、`~/.claude` 或其他固定目录。
- 每次确认下来的术语追加进去，下次开跑前先读一遍 —— 这是保证**跨批次术语一致**的务实手段
- 用户推翻过的译法（「这个词我们不这么说」）立刻记下来，下次默认采用
- 长期可沉淀为项目配置的 `glossary.md`（格式：源词 | locale | 译文 | 品类 | 备注）；若文件不存在，先创建模板或在输出中明确未启用词库。

## 注意

- 不要为了「忠实」输出读起来像机翻的句子；也不要为了「漂亮」改变事实
- 拿不准的法规问题，宁可保守并在「待确认」提出，不要赌
- 同一份文案发多个 locale 时，**逐 locale 单独跑**
- 本文档里的长度膨胀比例、字段上限、复数类别数均为**经验值与常见值**，以 CLDR / ICU / 平台后台 / 官方文本为准
